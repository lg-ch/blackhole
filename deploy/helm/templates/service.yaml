apiVersion: v1
kind: Service
metadata:
  name: {{ include "mangrove.fullname" . }}
  labels:
    {{- include "mangrove.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mangrove.selectorLabels" . | nindent 4 }}
